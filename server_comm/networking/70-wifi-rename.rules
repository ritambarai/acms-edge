# Rename the rtl8188eus USB WiFi dongle interface to wlan0.
# The 8188eu module registers a net device with SUBSYSTEM=net and driver=8188eu.
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="8188eu", NAME="wlan0"
